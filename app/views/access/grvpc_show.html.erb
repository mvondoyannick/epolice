<p id="notice"><%= notice %></p>

<h1 class="m-b-40">
  <%= link_to :back do %>
    <i class="fa fa-arrow-left"></i>
  <% end %>
  Compte utilisateur GRVPC
</h1>

<div class="row">
  <div class="col-sm-12">
    <table class="table table-borderless table-striped table-earning m-b-40">
      <thead>
      <th>Adresse email</th>
      <th>Téléphone</th>
      <th colspan="3"></th>
      </tr>
      </thead>

      <tbody>
      <% @grvpc.each do |affectations| %>
        <tr>
          <td><%= affectations.email %></td>
          <td><%= affectations.phone %></td>
          <td><%= link_to 'Show', nil %></td>
          <td><%= link_to 'Edit', nil %></td>
          <td><%= link_to 'Destroy', nil, class: 'btn btn-sm btn-danger' %></td>
        </tr>
      <% end %>
      </tbody>
    </table>

    <br>

    <%= link_to access_grvpc_new_path, class: 'btn btn-primary' do %>
      <%= fa_icon 'plus' %>
      Nouveau compte GRVPC
    <% end %>
  </div>
</div>