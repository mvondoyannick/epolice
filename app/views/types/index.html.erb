<% content_for(:title, 'TYPE EVENEMENTS')%>
<p id="notice"><%= notice %></p>
<%= link_to new_type_path, class: 'btn btn-primary m-b-40' do %>
  <i class="fa fa-plus"></i>
  Ajouter un type d'evenement
<% end %>
<h1 class="m-b-40">
  <%= link_to :back do %>
    <i class="fa fa-arrow-left"></i>
  <% end %>
  Types d'évenements
</h1>

<div class="col-lg-12">
  <div class="table-responsive table--no-card m-b-30">
    <table class="table table-borderless table-striped table-earning">
      <thead>
      <tr>
        <th>Name</th>
        <th>Description</th>
        <th>Partenaires</th>
        <th colspan="3"></th>
      </tr>
      </thead>
      <tbody>
      <% @types.each do |type| %>
        <tr>
          <td><%= type.name %></td>
          <td><%= type.description %></td>
          <td>
            <%= p = type.entity %></td>
          <td><%= link_to 'Show', type %></td>
          <td><%= link_to 'Edit', edit_type_path(type) %></td>
          <td><%= link_to 'Destroy', type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
      </tbody>
    </table>
  </div>
</div>

<br>

<%= link_to new_type_path, class: 'btn btn-primary' do %>
  <i class="fa fa-plus"></i>
  Ajouter un type d'evenement
<% end %>