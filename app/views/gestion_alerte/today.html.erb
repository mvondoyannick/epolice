<h1 class="subtitle is-3">Alertes de ce jour <%= Date.today.strftime("%D") %> </h1>
<hr style="background-color: #b9b7b7;" />
<p class="subtitle is-6"><i class="fa fa-hourglass-start" style="color: #000;"></i> Vous pouvez egalement  voir les alertes </p> 
  <%= link_to 'Hier', '#', class: 'button is-link is-outlined' %> <%= link_to "D'avant hier", '#', class: 'button is-link is-outlined' %> <%= link_to "D'il ya 3 jours", '#', class: 'button is-link is-outlined' %>
<hr style="background-color: #b9b7b7;" />
<div class="card">
      <header class="card-header">
        <p class="card-header-title">
          Liste d'alertes pour la date du <%= Date.today.strftime("%D") %>
        </p>
          <!--span class="icon">
            <i class="fa fa-angle-down" aria-hidden="false" style="color: #000000;"></i>
          </span-->
          <% if @today.nil? %>
            <%= link_to '#', class: 'button' do %>
              <i class="fa fa-map" aria-hidden="false" style="color: #000000;"></i>
              voir sur la carte
            <% end %>
          <% end %>
      </header>
      <div class="card-table">
        <div class="content">
          <table class="table is-fullwidth is-striped">
            <tbody>
              <% if !@today.nil? %>
                <tr>
                  <td width="5%"><i class="fa fa-bell-o"></i></td>
                  <td class="subtitle is-3"><i class="fa fa-bell-slash-o" style="color: #000000;"></i> Aucunes alertes pour le moment.</td>
                </tr>
              <% else %>
                <% @today.each do |alerte| %>
                  <tr>
                    <td width="5%"><i class="fa fa-bell-o" style="color: #000000;"></i></td>
                    <td>
                      <%= alerte.created_at.strftime("%B %d, %Y") %>
                    </td>
                    <td>
                      <%= alerte.titre %>
                    </td>
                    <td>
                      <%= link_to  "Détail", alerte_path(allerte), class: "button is-small is-primary" %>
                    </td>
                  </tr>
                <% end %>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
      <footer class="card-footer">
        <%= link_to  "Voir toutes les alertes", access_alertes_alerte_all_path, class: "card-footer-item" if @today.nil? %>
      </footer>
    </div>