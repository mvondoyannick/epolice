<p id="notice"><%= notice %></p>

<h1>Structures</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Raison</th>
      <th>Adresse</th>
      <th>Contactname</th>
      <th>Contantprenom</th>
      <th>Contactphone</th>
      <th>Contactemail</th>
      <th>Region</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @structures.each do |structure| %>
      <tr>
        <td><%= structure.name %></td>
        <td><%= structure.raison %></td>
        <td><%= structure.adresse %></td>
        <td><%= structure.contactname %></td>
        <td><%= structure.contantprenom %></td>
        <td><%= structure.contactphone %></td>
        <td><%= structure.contactemail %></td>
        <td><%= structure.region %></td>
        <td><%= link_to 'Show', structure %></td>
        <td><%= link_to 'Edit', edit_structure_path(structure) %></td>
        <td><%= link_to 'Destroy', structure, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Structure', new_structure_path %>
